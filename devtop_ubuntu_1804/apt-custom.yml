- name: Add an Apt signing key, uses whichever key is at the URL
  apt_key:
    url: "{{ url }}"
    state: present

- apt_repository:
    repo: "{{ repo_details }}"
    state: present
    filename: "{{ custom_file_name }}"
    update_cache: yes